/* ============================================================
   app.js — 엔트리포인트
   로그인/캐릭터 선택 없이 홈으로 바로 진입
   ============================================================ */

document.addEventListener("DOMContentLoaded", () => {
    navigate("home");
});
